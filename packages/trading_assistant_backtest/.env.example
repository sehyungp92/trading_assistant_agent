# Optional path to a sibling control-plane repo for local conformance tests.
TRADING_ASSISTANT_REPO=../trading_assistant

# Optional default planner mode. The current runner supports deterministic mode.
TRADING_ASSISTANT_BACKTEST_PLANNER_MODE=deterministic
